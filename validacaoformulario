Algoritmo "ValidacaoFormulario"

Var
   valor : real

Inicio
   repita
      escreval("Insira um valor maior ou igual a zero: ")
      leia(valor)
   ate valor >= 0
   escreval("Valor válido.")
FimAlgoritmo
