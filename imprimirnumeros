Algoritmo "ImprimirNumeros"

Var
   contador : inteiro
   saida : caractere

Inicio
   saida <- ""
   contador <- 1
   repita
      saida <- saida + contador + ", "
      contador <- contador + 1
   ate contador > 10
   escreval(saida)
FimAlgoritmo
